'use client';

import * as React from 'react';

interface MentionComboboxProps {
  editor: any;
}

export function MentionCombobox({ editor }: MentionComboboxProps) {
  // Simple implementation - can be enhanced later with actual mention functionality
  return null;
}